# WarLand — текущая работа и передача контекста

Обновлено 6 сентября 2026. Этот файл обновляется при существенных изменениях и перед завершением работы. Не считать его обещанием фонового выполнения после закрытия диалога.

## Запрос пользователя

Довести проект до рабочего релиза. Добавить регистрацию/вход. Затем выдать максимальную роль владельца игроку `egorkrid666` после защищённой привязки аккаунта. Обычные задачи выполнять автономно. Ник сам по себе не доказывает личность. Секреты не публиковать.

## Сейчас / точка продолжения

**Проверка ядра регистрации и подключение игрового допуска.**

- Рабочая интеграция: `development/initial-release` в https://github.com/ProkStudio/WarLand . `main` ещё не релиз.
- PR #9: https://github.com/ProkStudio/WarLand/pull/9 ; ветка `feature/auth-registration`.
- Код auth: Passwords, AuthRepository, Admission, Authentication. Добавлены AuthTest и AuthBoundaryTest. Последний кодовый коммит до документации: `5ee3ae9ea1f655c0d9ac890a0288a7492dcefcd0`.
- PBKDF2-SHA256/600000, ограниченный KDF-worker, SQL-регистрация, резерв владельца, одноразовый bootstrap, UUID/nonce-сессии и лимиты реализованы как движок. Это ещё НЕ /register или /login на сервере.
- Перед merge проверить оба GitHub verify на актуальном HEAD. Не приписывать результат старой ревизии новой. Статус и доказательства дополняются в PR #9.
- Следующая работа: [AUTHENTICATION](docs/AUTHENTICATION.md), раздел обязательной интеграции. Не начинать парольный слой заново.

Нужно подключить auth к CoreRuntime; отложить профили/деньги до входа; блокировать ВСЕ pre-auth действия (включая vanilla/alias-команды, чат, движение, инвентарь, урон, custom payload); скрыть ввод секретов из логов. Одного gate() недостаточно. При смене пароля согласовать отзыв всех сессий с login callback — CAS-ревизия сама этого не делает. Public offline-mode guard не снимать: обычный /login поверх незашифрованного offline-протокола небезопасен.

Владелец: сейчас CoreRuntime.staff использует vanilla PermissionLevel.OWNERS, отдельной OWNER-роли в Moderation нет. Привязать максимальные права только к сохранённому UUID и текущей авторизованной сессии. Проверить реальные packets/reconnect/crash, затем приватно создать bootstrap для `egorkrid666`. Производственный код не создан; OP/роль НЕ выданы.

## Последний завершённый срез рынка

[PR #8](https://github.com/ProkStudio/WarLand/pull/8) объединён, коммит `af2eafc1e89fb9f8bf549bba8289fb26f6149dac`. MarketRepository: intent/recovery, escrow/почта, атомарные сделки, комиссия, отмена/истечение и повторы. [MARKET_ESCROW](docs/MARKET_ESCROW.md).

Проверено на VPS: clean test build, Java 21/Fabric 1.21.11, 296 Java обнаружены (292 успешны, 4 старых пропуска), 30 Python успешны. Все 39 новых тестов рынка успешны, оба GitHub verify успешны. Снимки в crash-тестах синтетические, не Minecraft NBT. MarketFeature/InventoryGuard и финансовые флаги НЕ включены.

## Инфраструктурный блокер

Minecraft/root shell возвращает HTTP 429; GitHub доступен. В sandbox нет javac, локальная проба не состоялась. Не засчитывать это как тесты. До потери доступа Paper `minecraft.service` и исходный `warland-staging.service` были активны; мы их не перезапускали и не заменяли.

После восстановления shell сначала проверить:
- `/opt/warland-build/market-escrow-full.exit` — ранее подтверждено `java=0 python=0`;
- `market-escrow-smoke.exit`, `market-escrow-smoke.log`, `market-escrow-smoke/market-smoke-result.json` в `/opt/warland-build/` — результат запущенной дополнительной проверки ещё НЕ прочитан;
- отсутствие слушателя 127.0.0.1:25569 и состояние обоих исходных сервисов.

Smoke выполнялся ограниченным скриптом в отдельной синтетической копии. Не запускать повторно вслепую: каталог уже может существовать. `/opt/warland-build/market-escrow` содержит проверенный build и Java-файлы, которые могли остаться untracked: публикация сделана через GitHub после 429. Не делать reset/clean. Не затирать чужие dirty worktrees.

## Оставшиеся релизные ворота

1. Полная игровая авторизация и защищённый владелец.
2. ItemStack/NBT-кодек, durable player-data read-back, полный InventoryGuard и живая crash-матрица; затем GUI рынка, скупщик, почта и покупки.
3. Города/содержание/улучшения, дипломатия и игровые военные регрессии.
4. Клиентский модпак/ресурспак, техника/управление/HUD, ремонт, авиация и зачарования.
5. Крупные события, цепочки квестов, рейтинги и сезонные косметические награды.
6. Модерация/апелляции/античит/rollback, восстановление реальных данных.
7. Настоящая бета 10/20/30, профилирование/баланс, резервная копия и согласованный перевод Paper → Fabric с учётом UUID и авторизации.

Общий статус: интеграционная alpha. Не подменять релизные ворота зелёной компиляцией или idle TPS. Полный базовый статус: [RELEASE_STATUS](docs/RELEASE_STATUS.md).
