# WarLand — продолжение 2026-09-06

## Статус
Продолжение по прямому запросу владельца, 21:06 МСК. Полный stable-релиз НЕ готов. Файл сохраняет состояние при обрыве сессии; он не означает фоновую работу после окончания диалога.

- Доступ к GitHub работает. Доступ к VPS восстановлен после HTTP429; проверен в18:06UTC.
- Production warland-alpha.service работает; текущая версия по последнему deployment checkpoint — 0.1.0-alpha.3.1. В этой сессии сервер не перезапускался, аккаунты/миры/ключи/flags не менялись.
- Актуальное игровое ТЗ: PLAYER_EXPERIENCE_STATUS.md. Предыдущий auth hotfix: AUTH_WAIT_STATUS.md. RUN_STATE.md содержит более старый alpha3 baseline.
- main пока содержит документы. Полный проект находится в release/agent ветках; PR40 открыт и требует разрешения конфликтов, а не слепого merge.

## Найденная незавершённая работа
Локальный checkout `/opt/warland-build/lobby-rtp-20260906-1743`, ветка `agent/lobby-20260906-1722/safe-rtp`, содержит RtpService/RtpPolicy и регрессии. Завершённый QA-v1: exit1, Java512 detected /3failures /4skips. Это FAILED, не готовый артефакт. Требуется разобрать ошибки, сохранить исходные evidence и проверить исправление в отдельной копии.

Исходные checkout lobby-fortress/lobby-content/lobby-hud и старые dirty trees не изменять/не очищать. Общий build.lock использовать для тяжёлых проверок, production не использовать как тестовую среду. Перед публикацией/установкой — точный source/JAR, тесты, отдельная runtime-проверка; перед restart — игроки, private backup и code-only rollback без отката новых данных.

## Следующие шаги
1. Разобрать 3 RTP test failures и сохранить исправление/доказательства в отдельной ветке.
2. Проверить интеграцию RTP с текущим runtime и реальные ограничения телепортации; не объявлять source-only модуль работающим на сервере.
3. Продолжить согласованные lobby/auth/HUD/content работы с сохранением чужих изменений.
4. Собрать и проверить совместный candidate, затем безопасно подготовить следующую prerelease. Не перезаписывать существующие release tags/assets.

## Безопасность и границы
Никаких паролей, bootstrap, identity.bin, БД, playerdata, миров или приватных архивов в GitHub. Незавершённые market/покупки/capture не включать ради слова «релиз». Full stable #15, внешняя beta/нагрузка и игровой acceptance остаются открытыми.
